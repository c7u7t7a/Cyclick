// Copy this file to mapbox_token.dart and fill in your Mapbox public token.
// Get it at https://account.mapbox.com → Access tokens (use the pk. token only).
// The secret key (sk.) must NEVER be placed in client code.
const String kMapboxPublicToken = 'pk.YOUR_PUBLIC_TOKEN_HERE';
